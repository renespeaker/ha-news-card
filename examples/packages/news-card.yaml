# Package "News Card" – standard news sensors
#
# Include in configuration.yaml:
#   homeassistant:
#     packages: !include_dir_named packages
# …and copy this file to /config/packages/news-card.yaml.
#
# Requires the HACS integration "feedparser" (custom-components/feedparser).
#
# Naming: sensors are called "News <Name>" -> entity_id sensor.news_<name>.
# The News Card finds preset sensors via this convention automatically
# (preset: tagesschau -> sensor.news_tagesschau). Leave sensors you don't
# need commented out.

sensor:
  # ── Standard: national ────────────────────────────────────────────────
  # Tip: some feeds (e.g. tagesschau) send CORS headers and load directly in
  # the card without a sensor. Only add a sensor for feeds where the card
  # shows a CORS message – it names the exact sensor id to create.
  - platform: feedparser
    name: News Tagesschau
    feed_url: "https://www.tagesschau.de/index~rss2.xml"
    date_format: "%d.%m. %H:%M"
    local_time: true
    scan_interval:
      minutes: 30
    inclusions: [title, link, published]

  # Deutschlandfunk (preset: dlf) -> sensor.news_dlf
  # - platform: feedparser
  #   name: News DLF
  #   feed_url: "https://www.deutschlandfunk.de/nachrichten-100.rss"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # Deutsche Welle (preset: dw) -> sensor.news_dw
  # - platform: feedparser
  #   name: News DW
  #   feed_url: "https://rss.dw.com/rdf/rss-de-all"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # WELT (preset: welt) -> sensor.news_welt
  # - platform: feedparser
  #   name: News WELT
  #   feed_url: "https://www.welt.de/feeds/latest.rss"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # ── International ─────────────────────────────────────────────────────
  # Reuters has no official RSS anymore -> via Google News (preset: reuters)
  # - platform: feedparser
  #   name: News Reuters
  #   feed_url: "https://news.google.com/rss/search?q=Reuters&hl=de&gl=DE&ceid=DE:de"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # BBC News (preset: bbc) -> sensor.news_bbc
  # - platform: feedparser
  #   name: News BBC
  #   feed_url: "https://feeds.bbci.co.uk/news/world/rss.xml"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # The Guardian (preset: guardian) -> sensor.news_guardian
  # - platform: feedparser
  #   name: News Guardian
  #   feed_url: "https://www.theguardian.com/world/rss"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # Al Jazeera (preset: aljazeera) -> sensor.news_aljazeera
  # - platform: feedparser
  #   name: News Al Jazeera
  #   feed_url: "https://www.aljazeera.com/xml/rss/all.xml"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # Euronews (preset: euronews) -> sensor.news_euronews
  # - platform: feedparser
  #   name: News Euronews
  #   feed_url: "https://de.euronews.com/rss"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # ── Regional: uncomment the block you need ───────────────────────────
  # NRW (WDR) – active as the default region; swap for another block below.
  - platform: feedparser
    name: News WDR
    feed_url: "https://www1.wdr.de/uebersicht-100.feed"
    date_format: "%d.%m. %H:%M"
    local_time: true
    scan_interval:
      minutes: 30
    inclusions: [title, link, published]

  # Niedersachsen (NDR)
  # - platform: feedparser
  #   name: News NDR Niedersachsen
  #   feed_url: "https://www.ndr.de/nachrichten/niedersachsen/index-rss.xml"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # Schleswig-Holstein (NDR)
  # - platform: feedparser
  #   name: News NDR SH
  #   feed_url: "https://www.ndr.de/nachrichten/schleswig-holstein/index-rss.xml"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # Hamburg (NDR)
  # - platform: feedparser
  #   name: News NDR Hamburg
  #   feed_url: "https://www.ndr.de/nachrichten/hamburg/index-rss.xml"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # Mecklenburg-Vorpommern (NDR)
  # - platform: feedparser
  #   name: News NDR MV
  #   feed_url: "https://www.ndr.de/nachrichten/mecklenburg-vorpommern/index-rss.xml"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # Hessen (hessenschau)
  # - platform: feedparser
  #   name: News Hessenschau
  #   feed_url: "https://www.hessenschau.de/index.rss"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # Sachsen / Sachsen-Anhalt / Thüringen (MDR)
  # - platform: feedparser
  #   name: News MDR
  #   feed_url: "https://www.mdr.de/nachrichten/index-rss.xml"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # Berlin / Brandenburg (rbb24)
  # - platform: feedparser
  #   name: News Rbb24
  #   feed_url: "https://www.rbb24.de/aktuell/index.xml/feed=rss.xml"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # ── Google News ───────────────────────────────────────────────────────
  # Topmeldungen Deutschland (Preset: google_news)
  # - platform: feedparser
  #   name: News Google News
  #   feed_url: "https://news.google.com/rss?hl=de&gl=DE&ceid=DE:de"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # Lokal-Suche: Ort oder Begriff hinter q= eintragen (URL-kodiert).
  # Alternativ direkt in der Karte: google: "Mein Ort"
  # - platform: feedparser
  #   name: News Google Lokal
  #   feed_url: "https://news.google.com/rss/search?q=Meine%20Stadt&hl=de&gl=DE&ceid=DE:de"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]

  # ── Eigene Feeds: URL eintragen, Name frei wählen ────────────────────
  # - platform: feedparser
  #   name: Meine Lokalzeitung
  #   feed_url: "https://beispiel.de/rss.xml"
  #   date_format: "%d.%m. %H:%M"
  #   local_time: true
  #   scan_interval: { minutes: 30 }
  #   inclusions: [title, link, published]
